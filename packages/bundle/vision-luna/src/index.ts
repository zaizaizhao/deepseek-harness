/**
 * @deepseek-ai/dsh-vision-luna — static profile patch for the Luna vision Host
 * tool and browser intake adapter. The profile composer resolves
 * `cordis.patch.yml` through the manifest's `dsh.bundle.patch` field.
 * @module @deepseek-ai/dsh-vision-luna
 */

export {}
