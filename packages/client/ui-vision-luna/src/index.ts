/**
 * Luna vision intake plugin, Node half. Browser behavior ships from `./client`.
 * @module @deepseek-ai/dsh-client-ui-vision-luna
 */

/** Host plugin body; the feature's Host service is a separate package. */
export function apply(): void {}
